## 1. 模板引擎与渲染链路

- [x] 1.1 引入 Jinja2 依赖，并在脚本中提供统一模板渲染入口（替代当前正则模板渲染分支）。
- [x] 1.2 在统一渲染入口中注册 `prompt_file(path)` helper，支持单节点多 include 与重复 include。
- [x] 1.3 保留 `inputs` 上下文入口（`{{ inputs.<key> }}` 继续可用），并在缺失路径时快速失败。
- [x] 1.3a 渲染环境使用 Jinja2 `StrictUndefined`，确保未定义变量不被静默渲染为空字符串。
- [x] 1.4 保持既有 `inputs` 数据注入语义：非字符串值按 JSON 文本渲染到 prompt（避免 Python repr 回归）。
- [x] 1.5 明确实现边界：不新增旧语法特判、兼容分支、二次渲染或 AST 分析逻辑；字面量相对路径为主路径，动态/表达式路径不纳入兼容承诺。

## 2. 路径解析与最小校验

- [x] 2.1 `prompt_file` 路径基准实现为 `Path(config_path).resolve().parent`（不受 `cwd`、`run.project_root` 影响）。
- [x] 2.2 路径校验：仅允许非空相对路径，拒绝绝对路径；允许包含 `..` 的相对路径并按常规解析。
- [x] 2.3 文件读取：按 UTF-8 读取；文件不存在/不可读时失败。

## 3. 错误信息（极简）

- [x] 3.1 helper 调用错误处理：缺参、空字符串、非字符串参数时提供字段级错误。
- [x] 3.2 路径类错误输出保持最小：字段定位、失败原因；原始路径（可得时）与简短修复建议。
- [x] 3.3 不为错误信息增加复杂结构化字段契约（如强制 `config_dir/include_resolved`）。
- [x] 3.4 统一包装模板渲染错误，确保语法错误、未定义变量、helper 参数错误、文件读取失败时稳定输出失败节点定位（`node_id` 或 `workflow.nodes[n].prompt` 任一可用），不增加复杂结构化错误契约。

## 4. 测试覆盖（普通用户主路径）

- [x] 4.1 新增回归测试：无 include 的纯内联 `prompt` 仍可通过并保持既有 `inputs` 渲染。
- [x] 4.2 新增正向测试：一个 `prompt` 同时包含多个 include 与 `{{ inputs.* }}` 时可正确渲染执行。
- [x] 4.3 新增失败测试：helper 调用非法（缺参、空路径、非字符串参数）。
- [x] 4.4 新增失败测试：绝对路径、文件不存在。
- [x] 4.4b 新增失败测试：文件不可读时渲染失败（通过 mock/monkeypatch 稳定复现读取失败）。
- [x] 4.4a 新增回归测试：包含 `..` 的相对 include 路径可按配置目录规则解析。
- [x] 4.5 新增字段类型非法测试：`prompt` 非字符串或空字符串时应报错。
- [x] 4.6 新增回归测试：从不同 `cwd` 调用脚本时，include 解析结果一致。
- [x] 4.7 新增回归测试：`run.project_root` 变化时，仍以 workflow 配置文件目录解析 include。
- [x] 4.8 新增回归测试：`inputs` 非字符串值保持 JSON 文本注入语义（对象/数组场景）。
- [x] 4.9 新增失败测试：`inputs` 路径缺失时渲染失败，且报错定位到失败节点提示词字段（`node_id` 或 `workflow.nodes[n].prompt` 任一可用）。
- [x] 4.10 新增失败测试：模板语法错误与未定义变量错误时，报错稳定定位到失败节点提示词字段（`node_id` 或 `workflow.nodes[n].prompt` 任一可用）。
- [x] 4.11 新增回归测试：include 文件内容不做二次模板渲染（保持单次渲染模型）。

## 5. 预设与文档

- [x] 5.1 更新 `README.md` 配置说明，加入 `{{ prompt_file("<relative_path>") }}` 语法与路径规则。
- [x] 5.2 在文档中明确极简边界：不支持旧语法兼容、递归 include 检测、复杂路径策略。
- [x] 5.3 在文档中明确“字面量相对路径”为主路径，动态/表达式路径参数不纳入兼容承诺。
- [x] 5.3a 在文档中明确“include 文件内模板表达式不支持（按文本保留）”，并作为固定边界说明。
- [x] 5.3b 在文档中补充 Jinja2 `raw` 块示例，指导如何在主模板中输出字面量 `{{ ... }}`。
- [x] 5.4 在 `presets/` 增加或调整示例，展示“role include + 输出协议内联 + inputs 变量共存”的写法。
- [x] 5.5 补充示例 role 提示词文件，确保示例可被本地运行直接复用。
- [x] 5.6 在文档中补充“旧语法手动迁移”最小清单（仅替换写法与检查项，不引入兼容逻辑）。

## 6. 回归验证

- [x] 6.1 运行目标测试文件，确认新增用例通过且无回归。
- [x] 6.2 使用示例 preset 执行一次工作流，确认渲染后 prompt 与日志输出符合预期。

## 7. 明确忽略项（本提案不处理）

- 不做旧语法 `{{prompt_file:...}}` 的专门报错文案。
- 不做 include 递归检测与对应失败用例。
- 不为动态表达式路径 include 增加专门兼容实现与测试分支。
- 不做复杂路径安全策略扩展。

## 8. 提示词组织收敛（role 外置、输出协议内联）

- [x] 8.1 更新提案文档（proposal/design/spec），明确内置 preset 和 README 示例采用“仅 role 外置、输出协议内联”。
- [x] 8.2 调整 `presets/implement_loop.yaml` 与 `presets/refactor_loop.yaml`，将 `output_*` 提示词内容回收至节点 `prompt` 配置中。
- [x] 8.3 更新 `README.md` 示例，避免继续示范“role + output 协议双文件 include”。
- [x] 8.4 清理不再使用的 `presets/prompts/shared/output_*.md` 示例文件，并确认仓库内无引用残留。
